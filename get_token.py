#!/usr/bin/env python3
"""
飞书 Verification Token 获取助手
"""

print("""
╔════════════════════════════════════════════════════════════╗
║          如何获取飞书 Verification Token                   ║
╚════════════════════════════════════════════════════════════╝

📍 步骤1: 访问飞书开放平台
   https://open.feishu.cn/app

📍 步骤2: 选择您的应用
   应用名称: [您的应用名称]
   App ID: cli_a9fdafec72fbdcc4

📍 步骤3: 进入「事件订阅」页面
   左侧菜单 → 事件订阅

📍 步骤4: 查看 Verification Token
   在页面顶部会显示:
   ┌─────────────────────────────────────┐
   │ Verification Token: xxxxxxxxxxxxxx  │
   └─────────────────────────────────────┘

📍 步骤5: 复制 Token 并更新配置

════════════════════════════════════════════════════════════

""")

# 交互式更新
import os

token = input("请粘贴您的 Verification Token: ").strip()

if token:
    # 读取当前.env文件
    with open('.env', 'r') as f:
        lines = f.readlines()
    
    # 更新 token
    with open('.env', 'w') as f:
        for line in lines:
            if line.startswith('FEISHU_VERIFICATION_TOKEN='):
                f.write(f'FEISHU_VERIFICATION_TOKEN={token}\n')
            else:
                f.write(line)
    
    print(f"\n✅ Verification Token 已更新: {token}")
    print("\n配置文件已保存到: .env")
else:
    print("\n❌ 未输入 Token，配置未更新")
